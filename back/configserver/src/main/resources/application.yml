server:
  port: 8081

spring:
  cloud:
    config:
      server:

        git:
          uri: https://github.com/Yangdoubleh/malkongpuddingconfig
          default-label: main

# actuator 설정
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true